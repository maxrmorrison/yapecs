# Note that we do not need to set MODULE or CONFIG for an included config,
# just the values we want to include

# Whether to use today's temperature as a feature
TODAYS_TEMP_FEATURE = True

__NOT_VISIBLE = 42
